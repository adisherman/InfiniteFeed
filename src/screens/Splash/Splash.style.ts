import {StyleSheet} from 'react-native';

export const SplashStyles = StyleSheet.create({
  mainView: {
    padding: 0,
    height: '100%',
    width: '100%',
  },
  mainImage: {
    width: '100%',
    flex: 1,
    resizeMode: 'cover',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
});
